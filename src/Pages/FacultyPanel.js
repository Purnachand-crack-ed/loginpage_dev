import React from 'react';
import { Container, Typography } from '@mui/material';

const FacultyPanel = () => {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>Faculty Panel</Typography>
      <Typography>You can create and manage classes.</Typography>
    </Container>
  );
};

export default FacultyPanel;
